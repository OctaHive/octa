version: 1

vars:
  PROJECT: Octa

tasks:
  start:
    shell: echo Start building for project {{ PROJECT }}

  stop:
    shell: echo Stop building

  process:
    shell: echo Build

  build:
    cmds:
      - task: start
      - task: process
      - task: stop
