version: 1
concurrency: 2

# Run with: octa build
# Override for one run with: octa --concurrency 4 build
tasks:
  build:
    execute_mode: parallel
    cmds:
      - echo build frontend
      - echo build backend
      - echo build documentation
      - echo build packages
