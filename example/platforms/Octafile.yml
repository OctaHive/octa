version: 1

tasks:
  build_win:
    platforms: ["windows"]
    cmd: echo Windows build

  build_mac:
    platforms: ["macos/amd64"]
    cmd: echo Mac OS build

  build:
    cmds:
      - task: build_win
      - task: build_mac
